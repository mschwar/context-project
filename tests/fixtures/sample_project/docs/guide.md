# User Guide

## Getting Started

Run `python src/main.py` to see the greeting output.

## Functions

- `format_name(name)` — capitalizes and trims a name string
- `calculate_total(numbers)` — sums a list of integers
